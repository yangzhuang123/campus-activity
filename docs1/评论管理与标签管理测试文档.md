# 评论管理与标签管理 -- 测试文档

> 依据《评论管理与标签管理功能规格说明书》编写，覆盖评论管理、标签管理两大模块。

---

## 一、测试环境

| 项目 | 说明 |
|------|------|
| 后端框架 | Spring Boot 2.2.2 + Spring MVC + MyBatis-Plus |
| 前端技术 | HTML + CSS(Tailwind CSS) + JavaScript + Vue.js + Element UI |
| 数据库 | MySQL (springbootnp4n3) |
| 浏览器 | Chrome 最新版 |
| 测试类型 | 功能测试、界面测试、边界测试 |
| 测试方式 | 浏览器手工测试 + API 接口验证 |
| 测试日期 | 2026-04-28 |
| 应用地址 | http://localhost:8080/springbootnp4n3/ |

---

## 二、测试用例

### 模块五：评论管理

#### 5.1 评论管理功能

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|---------|--------|---------|---------|---------|---------|---------|---------|
| TC-0501 | 查看评论列表 | 用户已登录 | 1. 进入评论管理页面 | 无 | 评论管理页面正常加载，显示评论列表表格，包含评论内容、评论人、评论对象、点赞数、状态、创建时间、操作列 | 评论列表正常加载，显示评论内容、评论人、评论对象等列 | 通过 |
| TC-0502 | 按内容搜索 | 用户已登录，评论列表已加载 | 1. 在"评论内容"搜索框中输入关键词 2. 点击"搜索"按钮 | 关键词："活动" | 列表仅显示评论内容中包含"活动"的评论记录 | 搜索功能正常，返回匹配结果 | 通过 |
| TC-0503 | 按用户名搜索 | 用户已登录，评论列表已加载 | 1. 在"评论用户名"搜索框中输入用户名 2. 点击"搜索"按钮 | 用户名："张三" | 列表仅显示评论人为"张三"的评论记录 | 按用户名搜索正常 | 通过 |
| TC-0504 | 按类型筛选 | 用户已登录，评论列表已加载 | 1. 在"评论对象类型"下拉框中选择"社团活动" 2. 点击"搜索"按钮 | 类型：社团活动 | 列表仅显示objectType为"shetuanhuodong"的评论记录 | 筛选功能正常 | 通过 |
| TC-0505 | 查看评论详情 | 用户已登录，评论列表中有数据 | 1. 点击某条评论的"查看详情"按钮 | 无 | 弹出对话框，显示评论完整内容、评论人、评论对象、点赞数、状态、创建时间及回复列表 | 详情正常显示 | 通过 |
| TC-0506 | 用户删除自己的评论 | 用户已登录，评论列表中有自己发布的评论 | 1. 点击自己评论的"删除"按钮 2. 在确认弹窗中点击"确定" | 无 | 删除成功，评论从列表中移除，提示"删除成功" | 删除成功，API返回code:0 | 通过 |
| TC-0507 | 批量删除自己的评论 | 用户已登录，评论列表中有自己发布的多条评论 | 1. 勾选自己的多条评论 2. 点击"批量删除"按钮 3. 在确认弹窗中点击"确定" | 无 | 批量删除成功，所选评论从列表中移除，提示"删除成功"；无法选择他人评论进行删除 | 批量删除成功 | 通过 |
| TC-0508 | 分页功能 | 用户已登录，评论数据超过一页 | 1. 修改每页显示条数为5 2. 点击下一页 3. 点击上一页 | 每页显示：5条 | 分页正常，每页显示对应条数，翻页后数据正确加载，页码显示正确 | 分页参数正常工作 | 通过 |

#### 5.2 用户评论功能

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|---------|--------|---------|---------|---------|---------|---------|---------|
| TC-0509 | 发表评论 | 学生已登录，打开某活动详情页 | 1. 在评论输入框中输入评论内容 2. 点击"发布"按钮 | 评论内容："这个活动太棒了，强烈推荐！" | 评论发布成功，评论列表自动刷新，新评论显示在列表中，包含用户名、内容、时间、点赞数(0) | 初始因拦截器配置缺失导致"请先登录"，修复后API返回code:0，评论创建成功 | 已修复 |
| TC-0510 | 回复评论 | 学生已登录，活动详情页评论列表中有评论 | 1. 点击某条评论的"回复"按钮 2. 在回复输入框中输入回复内容 3. 点击"发布" | 回复内容："同意，我也想参加！" | 回复发布成功，回复显示在被回复评论下方，缩进展示，标注"回复 @用户名" | 带parentId的回复创建成功 | 通过 |
| TC-0511 | 编辑自己的评论 | 学生已登录，评论列表中有自己发布的评论 | 1. 点击自己评论的"编辑"按钮 2. 修改评论内容 3. 点击"保存" | 修改后内容："修改后的评论内容" | 评论修改成功，列表中显示更新后的内容 | 内容更新成功 | 通过 |
| TC-0512 | 删除自己的评论 | 学生已登录，评论列表中有自己发布的评论 | 1. 点击自己评论的"删除"按钮 2. 在确认弹窗中点击"确定" | 无 | 评论删除成功，该评论从列表中移除 | 删除成功 | 通过 |
| TC-0513 | 点赞评论 | 学生已登录，活动详情页评论列表中有评论 | 1. 点击某条评论的"点赞"图标 | 无 | 点赞成功，该评论点赞数+1，图标状态变为已点赞 | 点赞成功，like_count+1 | 通过 |
| TC-0514 | 取消点赞 | 学生已登录，已对某条评论点过赞 | 1. 再次点击该评论的"点赞"图标 | 无 | 取消点赞成功，该评论点赞数-1，图标状态恢复为未点赞 | 取消成功，like_count-1 | 通过 |
| TC-0515 | 查看活动评论列表 | 已登录，打开某活动详情页 | 1. 滚动到活动详情页评论区域 | 无 | 评论区正常加载，按时间倒序显示评论列表，每条评论包含用户头像、用户名、评论内容、发布时间、点赞数，默认显示10条 | 按时间倒序返回评论 | 通过 |
| TC-0516 | 查看评论回复 | 已登录，某条评论有回复 | 1. 展开某条顶级评论的回复区域 | 无 | 回复列表正常加载，按时间正序显示，回复缩进展示并标注"回复 @用户名" | 返回指定parentId的回复列表 | 通过 |
| TC-0517 | 空内容验证 | 学生已登录，打开某活动详情页 | 1. 评论输入框留空 2. 点击"发布"按钮 | 评论内容：空 | 提示"请输入评论内容"，评论不被提交 | 返回"评论内容不能为空" | 通过 |
| TC-0518 | 超长内容验证 | 学生已登录，打开某活动详情页 | 1. 在评论输入框中输入超过500字符的内容 2. 点击"发布"按钮 | 评论内容：超过500字符的文本 | 提示"评论内容不能超过500字符"或输入框限制最大输入长度为500字符 | 返回"评论内容不能超过500字符" | 通过 |
| TC-0519 | 未登录发表评论 | 未登录状态，打开某活动详情页 | 1. 在评论输入框中输入内容 2. 点击"发布"按钮 | 评论内容："测试评论" | 提示"请先登录"或跳转至登录页面 | 返回401"请先登录" | 通过 |
| TC-0520 | 删除他人评论 | 学生已登录，评论列表中有其他用户发布的评论 | 1. 尝试删除其他用户的评论 | 无 | 提示"无权删除此评论"，评论不被删除；或该评论不显示"删除"按钮 | 返回"无权删除此评论" | 通过 |

---

### 模块六：标签管理

#### 6.1 标签管理（社长操作）

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|---------|--------|---------|---------|---------|---------|---------|---------|
| TC-0601 | 查看标签列表 | 社长已登录 | 1. 进入标签管理页面 | 无 | 标签管理页面正常加载，显示标签列表表格，包含标签名称、标签类型、排序、状态、创建时间、操作列 | 标签列表正常加载 | 通过 |
| TC-0602 | 新增标签 | 社长已登录，进入标签管理页面 | 1. 点击"新增"按钮 2. 填写标签名称、选择标签类型、填写排序值 3. 点击"确定" | 名称：科技，类型：活动标签，排序：3，状态：正常 | 新增成功，标签列表中出现新标签"科技"，提示"操作成功" | 创建成功，API返回code:0 | 通过 |
| TC-0603 | 编辑标签 | 社长已登录，标签列表中有数据 | 1. 点击某个标签的"编辑"按钮 2. 修改标签名称和排序值 3. 点击"确定" | 名称修改为：体育运动，排序修改为：1 | 修改成功，标签列表中该标签信息已更新 | 更新成功 | 通过 |
| TC-0604 | 删除单个标签 | 社长已登录，标签列表中有数据 | 1. 点击某个标签的"删除"按钮 2. 在确认弹窗中点击"确定" | 无 | 删除成功，该标签从列表中移除，提示"删除成功" | 删除成功 | 通过 |
| TC-0605 | 批量删除标签 | 社长已登录，标签列表中有多条数据 | 1. 勾选多个标签 2. 点击"批量删除"按钮 3. 在确认弹窗中点击"确定" | 无 | 批量删除成功，所选标签从列表中移除 | 批量删除正常 | 通过 |
| TC-0606 | 按名称搜索 | 社长已登录，标签列表已加载 | 1. 在"标签名称"搜索框中输入关键词 2. 点击"搜索"按钮 | 关键词："体育" | 列表仅显示标签名称中包含"体育"的标签记录 | 搜索结果正确 | 通过 |
| TC-0607 | 按类型筛选 | 社长已登录，标签列表已加载 | 1. 在"标签类型"下拉框中选择"活动标签" 2. 点击"搜索"按钮 | 类型：活动标签 | 列表仅显示类型为"活动标签"的标签记录 | 返回指定类型标签 | 通过 |
| TC-0608 | 重复名称类型验证 | 社长已登录，已存在名称为"体育"、类型为"活动标签"的标签 | 1. 点击"新增"按钮 2. 填写相同的标签名称和类型 3. 点击"确定" | 名称：体育，类型：活动标签 | 提示"标签名称已存在"或"同一类型下标签名称不能重复"，标签不被创建 | 返回"标签名称已存在" | 通过 |
| TC-0609 | 分页功能 | 社长已登录，标签数据超过一页 | 1. 修改每页显示条数为5 2. 点击下一页 3. 点击上一页 | 每页显示：5条 | 分页正常，每页显示对应条数，翻页后数据正确加载，页码显示正确 | 分页正常 | 通过 |
| TC-0610 | 状态切换 | 社长已登录，进入标签管理页面 | 1. 点击"新增"按钮 2. 填写标签信息，状态选择"禁用" 3. 点击"确定" 4. 前台调用GET /tag/list/活动标签 | 名称：测试标签，类型：活动标签，状态：禁用 | 标签创建成功且状态为"禁用"；前台获取标签列表接口不返回该禁用标签 | 禁用标签不在公开列表中 | 通过 |

#### 6.2 活动标签绑定

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|---------|--------|---------|---------|---------|---------|---------|---------|
| TC-0611 | 绑定标签到活动 | 社长已登录，已有活动和标签数据 | 1. 调用POST /shetuanhuodong/bindTags接口 2. 传入活动ID和多个标签ID | activityId：活动ID，tagIds：[1, 2, 3] | 绑定成功，返回code:0，activity_tag关联表中新增对应记录 | 绑定成功 | 通过 |
| TC-0612 | 查看活动标签 | 某活动已绑定标签 | 1. 调用GET /shetuanhuodong/tags/{activityId}接口 | activityId：已绑定标签的活动ID | 返回该活动关联的所有标签列表，仅包含状态为"正常"的标签，按排序值正序排列 | 返回关联标签列表 | 通过 |
| TC-0613 | 更新活动标签 | 某活动已绑定标签 | 1. 调用POST /shetuanhuodong/bindTags接口 2. 传入活动ID和新的标签ID数组 | activityId：已绑定标签的活动ID，tagIds：[2, 4, 5] | 更新成功，旧的关联关系被替换为新的标签绑定，GET接口返回更新后的标签列表 | 旧绑定替换为新绑定 | 通过 |
| TC-0614 | 删除标签级联 | 某标签已与多个活动绑定 | 1. 社长在标签管理页面删除该标签 2. 查询activity_tag关联表 | 无 | 标签删除成功，activity_tag关联表中该标签的所有关联记录同步删除，不产生脏数据 | 标签删除后关联记录同步删除 | 通过 |
| TC-0615 | 按类型获取标签 | 已有多个不同类型和状态的标签 | 1. 调用GET /tag/list/活动标签接口（无需登录） | 类型：活动标签 | 返回所有类型为"活动标签"且状态为"正常"的标签列表，按排序值正序排列；禁用状态的标签不在返回结果中 | 返回正常状态标签 | 通过 |

---

## 三、测试用例汇总

| 模块 | 子模块 | 用例数量 | 通过 | 未通过 | 未测试 |
|------|--------|---------|------|--------|--------|
| 评论管理 | 评论管理功能 | 8 | 8 | 0 | 0 |
| 评论管理 | 用户评论功能 | 12 | 12 | 0 | 0 |
| 标签管理 | 标签管理（社长操作） | 10 | 10 | 0 | 0 |
| 标签管理 | 活动标签绑定 | 5 | 5 | 0 | 0 |
| **合计** | | **35** | **35** | **0** | **0** |

---

## 四、测试结论

- 总用例数：35
- 已执行：35
- 通过数：34（含1项修复后通过）
- 未通过数：0
- 未测试数：0
- 已执行通过率：**100%**（35/35）
- 总体通过率：**100%**（35/35）

---

## 五、缺陷记录

| 序号 | 用例编号 | 缺陷描述 | 严重程度 | 修复状态 |
|------|---------|---------|---------|----------|
| 1 | TC-0509 | InterceptorConfig.java未配置/discuss/\*\*、/tag/\*\*、/shetuanhuodong/\*\*拦截路径，导致认证拦截器未对新增端点生效，已登录用户发表评论时提示"请先登录" | 严重 | 已修复 |
| 2 | TC-0509 | DiscussController.save()方法未设置评论人用户名(userName)字段，导致评论记录中用户名为null | 一般 | 已修复 |

---

> 测试执行日期：2026-04-28
> 测试执行方式：浏览器手工测试（Chrome）+ API 接口验证
