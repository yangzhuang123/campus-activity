<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.XxiaoxiDao">

	<select id="selectListView" parameterType="map" resultType="com.entity.view.XxiaoxiView">
		SELECT 
			x.* 
		FROM 
			xiaoxi x
		WHERE 
			1=1
		<if test="yonghu != null">
			AND x.yonghu = #{yonghu}
		</if>
		<if test="yonghutable != null">
			AND x.yonghutable = #{yonghutable}
		</if>
		<if test="sort != null and sort != ''">
			ORDER BY ${sort} ${order}
		</if>
		<if test="sort == null or sort == ''">
			ORDER BY x.fabushijian DESC
		</if>
	</select>

	<select id="selectUnreadCount" parameterType="map" resultType="java.lang.Integer">
		SELECT 
			COUNT(*) 
		FROM 
			xiaoxi 
		WHERE 
			yonghu = #{yonghu} 
			AND yonghutable = #{yonghutable} 
			AND yuedu = 0
	</select>

	<select id="selectByYonghu" parameterType="map" resultType="com.entity.view.XxiaoxiView">
		SELECT 
			x.* 
		FROM 
			xiaoxi x
		WHERE 
			x.yonghu = #{yonghu} 
			AND x.yonghutable = #{yonghutable}
		ORDER BY 
			x.fabushijian DESC
	</select>

</mapper>
